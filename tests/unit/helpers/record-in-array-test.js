
import { recordInArray } from 'hgnapp/helpers/record-in-array';
import { module, test } from 'qunit';

module('Unit | Helper | record in array');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = recordInArray([42]);
  assert.ok(result);
});

