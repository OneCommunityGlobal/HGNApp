{{#if isHeaderVisible}}
  {{hgn-header isUserAdministrator=isUserAdministrator notifications = notifications userId=userId}}
{{/if}}
<div class="container">
  {{outlet}}
  <div id="destination">
    <!-- ember-wormhole -->
  </div>
</div>